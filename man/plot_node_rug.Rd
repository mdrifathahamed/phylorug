% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_node_rug.R
\name{plot_node_rug}
\alias{plot_node_rug}
\title{Draw the rug at every internal node of a plotted tree}
\usage{
plot_node_rug(
  npm,
  support = NULL,
  support_type = NULL,
  thresholds = NULL,
  cell_h,
  cell_w,
  n_cols,
  x_offset = 0,
  y_offset = 0,
  rug_position = c("inside", "outside"),
  last_pp = NULL
)
}
\arguments{
\item{npm}{The presence matrix from \code{\link[=node_presence_matrix]{node_presence_matrix()}}. One row per
internal backbone node (node numbers as rownames), one column per
comparison tree. Cells are \code{1} (recovered), \code{0} (not recovered), or a
proportion (pool recovery).}

\item{support}{Support matrix of the same shape as \code{npm}, or \code{NULL}. When
\code{NULL}, the rug is drawn in presence mode.}

\item{support_type}{Named character vector mapping each comparison tree to
its support measure (e.g. \code{"ufboot"}, \code{"sh_alrt"}, \code{"lpp"},
\code{"jackknife"}, \code{"bremer_ratio"}), or \code{NULL}. When \code{NULL}, values >1
are auto-normalized to 0-1 and binned against universal thresholds.}

\item{thresholds}{Optional list overriding the built-in bin thresholds, keyed
by support type.\code{NULL} uses the literature defaults.}

\item{cell_h, cell_w}{Numeric. Height and width of one cell, in the tree's
plotting coordinates.}

\item{n_cols}{Integer. Columns in each node's grid.}

\item{x_offset, y_offset}{Numeric. Shift the whole grid away from the node, as
a fraction of the tree's width and height.}

\item{rug_position}{One of \code{"outside"} or \code{"inside"}(default).}

\item{last_pp}{Plot coordinates from \code{\link[ape:plot.phylo]{ape::plot.phylo()}} giving the x/y
position of every node and tip, used to place each rug grid at its node.
\code{\link[=plot_phylorug]{plot_phylorug()}} always supplies this. If called directly with \code{NULL},
the coordinates are fetched from the most recently drawn tree.}
}
\value{
Returns nothing; it draws the rug cells directly onto the tree.
}
\description{
For each internal node of the backbone tree, paints a small grid of
rectangles, one per comparison tree, showing whether that tree recovered the
node's clade and, in support mode, how strongly. Together these grids are the
rug.

The appearance is set by a tier, resolved from which arguments are supplied
rather than named directly. (\code{\link[=plot_phylorug]{plot_phylorug()}} selects the tier for the user
through its \code{mode} argument.)
\itemize{
\item Tier 1, presence: \code{support} is \code{NULL}. Cells are black for present,
white for absent, and grey for partial recovery in a pool.
\item Tier 2, support: both \code{support} and \code{support_type} are supplied.
Recovered cells are shaded by binned support strength, from black (very
high) through greys to yellow (low). A cell is white when the tree does
not recover the clade at all, and red when the tree recovers the clade
but carries no support value for it (an unscored node).
}

Users do not call this directly; \code{\link[=plot_phylorug]{plot_phylorug()}} calls it after drawing the
tree and working out the cell geometry.
}
\keyword{internal}
