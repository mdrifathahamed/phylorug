% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_phylorug.R
\name{plot_phylorug}
\alias{plot_phylorug}
\title{Draw a phylorug: a backbone tree with node rugs}
\usage{
plot_phylorug(
  backbone,
  npm,
  file = NULL,
  width = NULL,
  height = NULL,
  mode = c("presence", "support"),
  support_idx = 1,
  thresholds = NULL,
  n_rows = NULL,
  n_cols = NULL,
  include_backbone = FALSE,
  nodes = NULL,
  legend = TRUE,
  show_support = FALSE,
  show_support_idx = 1,
  cell_scale = 0.45,
  x_offset = 0,
  y_offset = 0,
  rug_position = c("inside", "outside"),
  dot_identical = TRUE,
  dot_col = "black",
  dot_cex = NULL,
  support_label_cex = NULL,
  support_label_col = "red",
  rug_on_identical = FALSE,
  hide_unsupported = TRUE,
  ...
)
}
\arguments{
\item{backbone}{A \code{phylo} object representing the backbone tree.}

\item{npm}{A named list containing the \code{presence} and \code{support} matrices,
returned by \code{\link[=node_presence_matrix]{node_presence_matrix()}}.}

\item{file}{Optional character string specifying the output file name or full
directory path. If left as \code{NULL} (the default), the plot renders in the
active graphics device (e.g., RStudio) for quick drafting. To avoid aspect
ratio distortion caused by GUI exports and to generate perfectly scaled,
publication-ready figures, provide a file path here (must end in \code{.pdf},
\code{.png}, or \code{.jpg}) to utilize the package's internal scaling engine.}

\item{width, height}{Numeric. Optional canvas dimensions in inches, applied
only when exporting to a \code{file}.If left as \code{NULL} (the default), the
package's internal engine dynamically calculates the optimal canvas
dimensions based on the tree size and legend layout. Providing values here
overrides the automatic scaling, which is useful for meeting strict journal
dimension requirements.}

\item{mode}{One of \code{"presence"} (default) or \code{"support"}.}

\item{support_idx}{Integer (1, 2, or 3). Default is \code{1}. Specifies which
single support matrix from the \code{npm} list to visualize. For example, if you
generated the data using \code{support_col = c(1, 2)}, passing \code{2} here tells the
plotting engine to physically shade the grid cells using the second metric
(stored in your list as \code{support_2}).}

\item{thresholds}{Optional list overriding built-in bin thresholds. Keyed
by metric name for named metrics, or \code{"universal"} when \code{support_type}
is not declared. For example:
\code{thresholds = list(ufboot = c(very_high = 97, high = 85, moderate = 50))}
or \verb{thresholds = list(universal = c(very_high = 0.90, high = 0.70,}
\verb{moderate = 0.50))}. If \code{NULL} (default), literature-based thresholds
are applied.}

\item{n_rows, n_cols}{Integer. Grid shape for the rug at each node. If
\code{NULL} (the default), a roughly square grid is chosen automatically.}

\item{include_backbone}{Logical or character. If \code{FALSE} (default), the
backbone does not occupy a rug cell. If \code{TRUE}, the backbone is added as
cell 1 using universal thresholds in support mode (with a message). If a
character string naming a support metric (\code{include_backbone = "ufboot"}),
the backbone is added as cell 1 and binned against that metric's
thresholds.}

\item{nodes}{Optional. Restricts the plot to a subset of backbone internal
nodes. Accepts any one of:
\itemize{
\item A \strong{list of character vectors}, the recommended way to select
clades: each element gives >= 2 tip labels from \code{backbone$tip.label},
and phylorug resolves each set to its most recent common ancestor
(MRCA) internally. For example
\code{nodes = list(c("sp_A", "sp_B"), c("sp_C", "sp_D", "sp_E"))} selects
two clades by the taxa that define them -- no node IDs required.
\item A numeric vector of node IDs matching \code{rownames(npm$presence)}
(ape's internal node numbering, \code{Ntip(backbone) + 1} upward).
\item A character vector of labels matching \code{backbone$node.label}.
}
If \code{NULL} (the default), every internal node in \code{npm} is eligible for a
dot or rug, subject to \code{dot_identical} and \code{hide_unsupported}. When
supplied, only the selected nodes are considered at all: unselected
nodes get no dot, rug, or support label, regardless of those other
settings. An unresolvable node ID, label, or taxon name raises an error
naming the offending value(s).}

\item{legend}{Logical. Draw the legend. Default TRUE.}

\item{show_support}{Logical. If \code{TRUE}, backbone node support labels are
drawn beside each node. Default \code{FALSE}.}

\item{show_support_idx}{Integer or NULL. Which value from compound node
labels (e.g. "80/95") to display when \code{show_support = TRUE}. Default \code{1}
(first value). Set to \code{NULL} to display the full compound label as-is.}

\item{cell_scale}{Numeric multiplier on cell height. Default 0.45.}

\item{x_offset, y_offset}{Numeric grid shift. Default 0.}

\item{rug_position}{One of \code{"inside"} (default) or \code{"outside"}. Controls
where the node rug grid is placed relative to the backbone node: \code{"inside"}
tucks the grid into the crook above-left (toward the root), while
\code{"outside"} places the grid to the right of the node (toward the tips).}

\item{dot_identical}{Logical. Default \code{TRUE}. If \code{TRUE}, draws a small dot on
backbone nodes where every comparison tree is identical in recovering the
clade.}

\item{dot_col, dot_cex}{Colour and size of the identical-clade dot. Defaults
are \code{"black"} and NULL (auto-scales).}

\item{support_label_cex}{Numeric or \code{NULL}. Size of the backbone support
labels. Default \code{NULL} auto-scales with tree size.}

\item{support_label_col}{Colour of the backbone support labels. Default
\code{"red"}.}

\item{rug_on_identical}{Logical. Default \code{FALSE}. When this and
\code{dot_identical} are both \code{TRUE}, unanimous nodes receive both the dot and
a support rug, making per-tree support strength visible even at universally
recovered clades.}

\item{hide_unsupported}{Logical. Default \code{TRUE}. Nodes where no comparison
tree recovers the clade are left bare, the absence of both a dot and a
rug signals that the clade is unique to the backbone topology. Set to
\code{FALSE} to draw all-white rugs at these nodes, which can be useful when
you want every internal node to carry a visible grid for annotation or
figure editing.}

\item{...}{Additional arguments passed to \code{\link[ape:plot.phylo]{ape::plot.phylo()}}, such as
\code{cex}, \code{edge.width}, \code{font}, or \code{label.offset}. These override the
automatic scaling when provided.}
}
\value{
Invisibly, the file path if a file was written, or \code{NULL} if plotted
directly to the active graphics device (not recommended).
}
\description{
\code{\link[=plot_phylorug]{plot_phylorug()}} overlays clade stability grids (rugs) on a backbone
phylogeny, comparing how multiple analyses treat each internal node.
In presence mode, cells are black (recovered) or white (absent). In
support mode, cells are shaded by binned support strength. The function
handles canvas sizing, legend placement, and font scaling automatically.
}
\examples{
# Build the plotting input from real trees:
backbone <- sample_trees[["70p_uce"]]
others   <- sample_trees[names(sample_trees) != "70p_uce"]
support_type <- c(
  "70p_ASTRAL_partition_entropy" = "lpp",
  "70p_ASTRAL_uce"               = "lpp",
  "70p_ghost"                    = "ufboot",
  "70p_partition_entropy"        = "ufboot"
)
npm_st <- node_presence_matrix(backbone, others,
                               support_col = 1,
                               support_type = support_type)

# --- Presence mode --------------------------------------------------------
# Each cell shows whether an analysis recovered the backbone clade.
tmp <- tempfile(fileext = ".pdf")
plot_phylorug(backbone, npm_st, file = tmp)
unlink(tmp)

# --- Support mode ---------------------------------------------------------
tmp2 <- tempfile(fileext = ".pdf")
plot_phylorug(backbone, npm_st,
              file         = tmp2,
              mode         = "support",
              support_idx  = 1)
unlink(tmp2)

# --- Some optional controls -----------------------------------------------
tmp3 <- tempfile(fileext = ".pdf")
plot_phylorug(backbone, npm_st,
              file             = tmp3,
              include_backbone = TRUE,
              rug_position     = "outside")
unlink(tmp3)
#' # --- Restrict to specific nodes ---------------------------------------------
# By taxa (recommended): select a clade by the tips that define it.
tmp4 <- tempfile(fileext = ".pdf")
plot_phylorug(backbone, npm_st,
              file  = tmp4,
              nodes = list(backbone$tip.label[1:2]))
unlink(tmp4)

# By node ID, if you already know it (see rownames(npm_st$presence)):
tmp5 <- tempfile(fileext = ".pdf")
plot_phylorug(backbone, npm_st,
              file  = tmp5,
              nodes = as.integer(rownames(npm_st$presence))[1:2])
unlink(tmp5)
}
\seealso{
\code{\link[=node_presence_matrix]{node_presence_matrix()}} to build the input data,
\code{\link[=check_taxa]{check_taxa()}} to verify taxon sets, and
\code{plot_node_rug()} which handles the cell-level
drawing(not used by the user).
}
