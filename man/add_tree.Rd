% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_tree.R
\name{add_tree}
\alias{add_tree}
\title{Add a comparison tree to an existing node presence matrix}
\usage{
add_tree(
  npm,
  backbone,
  new_tree,
  name,
  support_col = 1,
  support_type = NULL,
  pool_threshold = 1
)
}
\arguments{
\item{npm}{A node presence matrix list, as returned by
\code{\link[=node_presence_matrix]{node_presence_matrix()}} or a previous call to \code{add_tree()}.}

\item{backbone}{The backbone tree used to create \code{npm}. Must be the same
\code{phylo} object (same topology and tip labels) that was passed to
\code{\link[=node_presence_matrix]{node_presence_matrix()}}.}

\item{new_tree}{A \code{phylo} or \code{multiPhylo} object representing the new
comparison analysis to add. Must be rooted.}

\item{name}{Character string. Column name for the new tree in the npm
matrices. Required. Must be a single non-empty string and must not
duplicate an existing column name.}

\item{support_col}{Integer or vector of integers (max 3). Which support
value(s) to extract from the new tree's node labels, matching the
\code{support_col} used when the original \code{npm} was built. Default is \code{1}. For
example, if the new tree is from IQ-TREE with compound labels \code{"80/95"},
passing \code{c(1, 2)} extracts both metrics.}

\item{support_type}{Optional character string naming the support metric
of the new tree (e.g. \code{"ufboot"}, \code{"lpp"}, \code{"jackknife"}). If supplied, it
is appended to the npm's \code{"support_type"} attribute so that
\code{\link[=plot_phylorug]{plot_phylorug()}} can apply metric-specific thresholds automatically. If
\code{NULL}, the new tree's support values will be auto-normalized (values >1
divided by 100) and binned against universal thresholds at plot time. For
this reason, raw Bremer support (an unbounded integer) is not supported;
use the Bremer ratio \code{"bremer_ratio"} instead, which is bounded between
0 and 1.}

\item{pool_threshold}{Numeric between 0 and 1. Controls how pools are
scored. Default \code{1.0} (strict consensus). Should match the value used
when the original npm was built. See \code{\link[=node_presence_matrix]{node_presence_matrix()}} for
details.This parameter is only applicable when evaluating
parsimony-based \code{multiPhylo} objects (e.g., Most Parsimonious Trees
generated via TNT or similar software).}
}
\value{
The updated npm list with the new tree appended as an additional
column in the \code{presence} matrix and each \verb{support_*} matrix. Attributes
\code{"node_id"} and \code{"pool_sizes"} are updated, and \code{"support_type"} is
extended if the argument was supplied.
}
\description{
Adds a new comparison tree to an existing node presence matrix without
rerunning \code{\link[=node_presence_matrix]{node_presence_matrix()}} from scratch. The function computes
clade presence and support for the new tree only and attaches the results as
a new column in every matrix of the npm. All existing columns remain
unchanged.
}
\details{
Three taxon scenarios are handled automatically:
\describe{
\item{Identical}{The new tree shares all backbone taxa exactly. Every
clade is evaluated normally.}
\item{Superset}{The new tree contains all backbone taxa plus extras.
The extra taxa are pruned internally so that clade keys match the
backbone's. Without this pruning, a backbone clade \verb{(A, B)}
would fail to match a new-tree node grouping \verb{(A, B, X)},
producing a false absence. A message reports the pruning. The
user's original tree object is not modified.}
\item{Missing}{The new tree lacks one or more backbone taxa. Any
backbone clade containing a missing taxon cannot be evaluated in
this tree and is set to \code{NA} in both the presence and support
matrices. These appear as red ("not computed") cells in the rug
plot. A message lists the missing taxa and suggests rebuilding
the npm from scratch with \code{\link[=prune_to_shared]{prune_to_shared()}} if the red cells
are undesirable. Unlike \code{\link[=node_presence_matrix]{node_presence_matrix()}}, which errors
on missing taxa, \code{add_tree()} is deliberately permissive:
when adding a tree after the initial analysis, different taxon
sampling is a legitimate use case, not a data error.}
}

The new tree must be rooted. If it is a \code{multiPhylo} object (a pool
of equally optimal trees from one search), clade presence is determined
by \code{pool_threshold}: the default (\code{1.0}, strict consensus) requires the
clade in every pool tree; \code{0.5} applies majority rule; \code{0} records the
raw proportion. Support extraction is skipped for pools because
averaging node labels across equally optimal trees is not scientifically
valid; support cells for that column will be \code{NA}. To include support
values from a parsimony analysis, compute the consensus tree with
bootstrap or jackknife support upstream (e.g. in TNT) and pass that
consensus as a single \code{phylo} object.

If \code{support_type} is supplied, it is appended to the
\code{"support_type"} attribute stored in the npm object, keyed by the
new tree's name. This allows \code{\link[=plot_phylorug]{plot_phylorug()}} to apply the correct
metric-specific thresholds without the user re-declaring the full vector.
}
\examples{
# Build an npm with 3 of the 5 shipped trees:
backbone <- sample_trees[["70p_uce"]]
others   <- sample_trees[c("70p_partition_entropy", "70p_ghost")]
npm      <- node_presence_matrix(backbone, others)
ncol(npm$presence)   # 2 comparison trees

# Add a fourth tree without re-running the pipeline:
npm <- add_tree(npm, backbone,
                sample_trees[["70p_ASTRAL_uce"]],
                name = "ASTRAL_uce")
ncol(npm$presence)   # now 3

# Add the fifth tree with a declared support type:
npm <- add_tree(npm, backbone,
                sample_trees[["70p_ASTRAL_partition_entropy"]],
                name = "ASTRAL_part_entropy",
                support_type = "lpp")
ncol(npm$presence)   # now 4
attr(npm, "support_type")

# To add a tree from an external file (not run):
#   new_tree <- ape::read.tree("path/to/new_analysis.treefile")
#   new_tree <- ape::root(new_tree, outgroup = "outgroup_sp",
#                         resolve.root = TRUE)
#   new_tree <- ape::drop.tip(new_tree, "outgroup_sp")
#   npm <- add_tree(npm, backbone, new_tree, name = "new_analysis",
#                   support_type = "ufboot")
}
\seealso{
\code{\link[=node_presence_matrix]{node_presence_matrix()}} to build the initial npm, \code{\link[=plot_phylorug]{plot_phylorug()}}
to visualise the result, and \code{\link[=check_taxa]{check_taxa()}} to diagnose taxon overlap
before adding.
}
