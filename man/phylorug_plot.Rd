% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/phylorug_plot.R
\name{phylorug_plot}
\alias{phylorug_plot}
\title{Create a layered phylorug plot object}
\usage{
phylorug_plot(
  backbone,
  npm,
  mode = c("presence", "support"),
  support_idx = 1,
  support_type = NULL,
  thresholds = NULL,
  include_backbone = FALSE,
  layout = c("phylogram", "cladogram", "fan", "unrooted"),
  file = NULL,
  width = NULL,
  height = NULL,
  ...
)
}
\arguments{
\item{backbone}{A \code{phylo} object.}

\item{npm}{A named list from \code{\link[=node_presence_matrix]{node_presence_matrix()}} containing at least
a \code{presence} matrix.}

\item{mode}{\code{"presence"} or \code{"support"}.}

\item{support_idx}{Integer. Which support matrix to visualize. Default \code{1}.}

\item{support_type}{Named character vector mapping trees to support types.}

\item{thresholds}{Optional list overriding bin thresholds.}

\item{include_backbone}{Logical. Include backbone as cell 1. Default \code{FALSE}.}

\item{layout}{Tree layout: \code{"phylogram"} (default, with branch lengths),
\code{"cladogram"} (equal spacing), \code{"fan"} (circular), or \code{"unrooted"}.}

\item{file}{Output file path (\code{.pdf}, \code{.png}, \code{.jpg}), or \code{NULL} for
on-screen rendering.}

\item{width, height}{Canvas dimensions in inches. \code{NULL} for auto.}

\item{...}{Additional arguments passed to \code{\link[ape:plot.phylo]{ape::plot.phylo()}}, such as
\code{cex}, \code{font}, \code{edge.width}, \code{edge.color}, \code{tip.color},
\code{label.offset}, or \code{show.tip.label}.}
}
\value{
An S3 object of class \code{"phylorug_plot"}. Add layers with \code{+}.
}
\description{
Builds a composable plot object that can be extended with \code{+} to add
visual layers (rugs, dots, labels, legends). Nothing is drawn until
the object is printed or explicitly plotted.
}
\examples{
backbone <- ape::read.tree(text = "(((A,B),C),(D,E));")
npm <- list(
  presence = matrix(c(1,0,1,1, 1,1,0,1), nrow = 4, ncol = 2,
    dimnames = list(as.character(6:9), c("tree_1", "tree_2")))
)

# Build and render
p <- phylorug_plot(backbone, npm) +
  rug_layer() +
  dot_layer()
\dontrun{
p   # prints to screen
}
}
