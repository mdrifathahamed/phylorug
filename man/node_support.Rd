% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/node_support.R
\name{node_support}
\alias{node_support}
\title{Parse support values from internal nodes of a phylogenetic tree}
\usage{
node_support(tree, sep = "/", digits = NULL)
}
\arguments{
\item{tree}{A phylogenetic tree of class \code{"phylo"}, with node labels stored
in \code{tree$node.label}.}

\item{sep}{A single character string giving the delimiter that separates
compound support values. Defaults to \code{"/"}.}

\item{digits}{Integer or \code{NULL}. If supplied, support values are rounded to
this many decimal places. Defaults to \code{NULL}.}
}
\value{
A data frame with three columns, \code{support_1}, \code{support_2} and
\code{support_3}, and one row per internal node of the tree. Columns not present
in a given label are \code{NA}.
}
\description{
Extracts the node labels of one tree and returns their support values as a
data frame: one row per internal node, three columns (\code{support_1},
\code{support_2}, \code{support_3}). How a label fills those columns:
\itemize{
\item A single value (such as an IQ-TREE bootstrap) fills \code{support_1}; the other
two are \code{NA}.
\item A compound label split by \code{sep} (default \code{/}) fills one column per value:
\itemize{
\item Two for IQ-TREE SH-aLRT/UFBoot2 or ASTRAL pp1/pp2.
\item Three for IQ-TREE SH-aLRT/UFBoot2/aBayes or ASTRAL pp1/pp2/pp3.
}
\item Empty or non-numeric pieces become \code{NA}.
\item A tree with no node labels returns a data frame of all \code{NA}.
}
}
\keyword{internal}
